#include "main.h"

/**
 * _puts_recursion - check code
 * @s: string to display
 * Return: string length
 */
int _puts_recursion(char *s)
{
	int len = 0;

	while (*(str + len) != '\0')
		len++;
	return (write(1, str, length));
}
